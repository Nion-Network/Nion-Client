<script lang="ts">

    /*
    const socket = new WebSocket(`ws://${$settingsStore.connectionNode}`)
    let messageHistory: [string] = []
    socket.onmessage = (message) => {
        const data: string = message.data as string
        messageHistory = [...messageHistory, `${data.substring(0, 20)}...`]
    }
    */
</script>
This a dashboard
<!--
<ul>
    {#each messageHistory as message (message)}
        <li>{message}</li>
    {/each}
</ul>

-->